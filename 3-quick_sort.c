#include "sort.h"

/**
 *quick_sort - partition array
 *@array: random number array
 *@size: array size
 *Return: the value of the pivot
 */
void quick_sort(int *array, size_t size)
{
	if (array || size)
	{
		return;
	}
}
